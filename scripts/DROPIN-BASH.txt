# Paste into Git Bash (Windows) — SymbolAlgebra

## 31 compile errors after sync-dropins? Run this first

`sync-dropins.sh import` only copies 33 flat files. If you were on
`cursor/working-hand-gameplay-c55a`, it left 7 extra files behind and
overwrote the rest — that causes ~31 Unity errors.

**Fix — restore the full Scripts folder from GitHub:**

```bash
cd /c/Users/rober/SymbolAlgebra
git fetch origin
bash scripts/restore-scripts.sh
```

Or without the script:

```bash
cd /c/Users/rober/SymbolAlgebra
git fetch origin
git checkout origin/main -- Assets/DragonBoxAlgebra/Scripts/
```

Then open Unity and wait for compile.

---

## Do NOT use drop-in import after git pull

Prefer **git only**:

```bash
cd /c/Users/rober/SymbolAlgebra
git fetch origin
git pull origin main
```

Only use `bash scripts/sync-dropins.sh import` if you copied an old
`dropins/` folder manually and do not use git.

---

## Manual drop-in (no script file)

```bash
cd /c/Users/rober/SymbolAlgebra
S=Assets/DragonBoxAlgebra/Scripts
mkdir -p "$S/Core" "$S/Gameplay" "$S/UI" "$S/Audio"
cp dropins/AlgebraBootstrap.cs "$S/"
cp dropins/AlgebraBoard.cs dropins/BoardCard.cs dropins/BoardSide.cs dropins/CardKind.cs dropins/CombineRules.cs dropins/WinChecker.cs "$S/Core/"
cp dropins/AlgebraGameController.cs dropins/BalancePending.cs dropins/BoardVisualRules.cs dropins/CardFlipRules.cs dropins/CoordinatedCreatureThemes.cs dropins/GameSnapshot.cs dropins/HandCompositionRules.cs dropins/HandRules.cs dropins/HandVisualRules.cs dropins/LevelDefinition.cs dropins/LevelGenerator.cs dropins/LevelLibrary.cs dropins/LevelSolvabilityRules.cs dropins/MoveTracker.cs dropins/PendingCancelMarker.cs dropins/ThemeAssignment.cs "$S/Gameplay/" 2>/dev/null || true
cp dropins/AudioManager.cs "$S/Audio/"
cp dropins/BoardSideLayout.cs dropins/BoardView.cs dropins/AlgebraUI.cs dropins/AsteriskCancelWidget.cs dropins/BalanceHoleWidget.cs dropins/BoardDropZone.cs dropins/CardSpriteLoader.cs dropins/CardVisuals.cs dropins/CardWidget.cs dropins/CreatureArt.cs dropins/CreatureReaction.cs dropins/EmojiFont.cs dropins/HandView.cs dropins/LevelCompleteView.cs dropins/SpriteFactory.cs dropins/VortexEffect.cs "$S/UI/" 2>/dev/null || true
echo "Done"
```

---

## After fixing

1. Unity → wait for compile
2. Scene: `Assets/DragonBoxAlgebra/Scenes/DragonBox.unity`
3. Press **Play**

Latest main includes:
- Hand gameplay branch (distinct themes, matching pairs)
- Right side fits up to 6 tiles
- Balance defer + bash restore script
