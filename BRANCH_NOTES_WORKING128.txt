# Combined: correct hand images + hand drag + scene drag

Branch: `cursor/working--scene-drag-1412`
Alias:  `cursor/working128-correct-images-1412` (same tip)

## Sources

| Branch | Kept from it |
|--------|----------------|
| `cursor/working128variable-images--x` | Correct hand tile count + PNG images, flippable hand tiles, dual-hand Ch7, CreatureSprites |
| `cursor/working-except-for-scene-drag-not-yet` | Hand→scene drag survival (`HandView` does not rebuild mid-drag; EventSystem drag threshold 150) |
| Scene opposite-snap (from working128--x scene path; remote had no `working--scene-drag-1412` tip yet) | `DraggableTile` never reparents under target; board end-drag merge + hover fallback |

## Pull

```bash
cd /c/Users/rober/SymbolAlgebra
git merge --abort 2>/dev/null
git fetch origin
git checkout cursor/working--scene-drag-1412
git reset --hard origin/cursor/working--scene-drag-1412
```

Or:

```bash
git checkout cursor/working128-correct-images-1412
git reset --hard origin/cursor/working128-correct-images-1412
```
