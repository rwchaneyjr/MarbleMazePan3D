# Step 1 — correct hand images + scene opposite drag

Branch: `cursor/working--scene-drag-1412`
Same tip: `cursor/working128-correct-images-1412`
Base tip: `e5452db` = `cursor/working128variable-images--x`

## This step only

Started from the branch with **correct hand images / tile count / flippable tiles**:
`cursor/working128variable-images--x`

Kept its **scene opposite-drag** (board tile → opposite → swirl, no snap reparent / ghost freeze).

**Not added yet** (next step):
- Hand→scene drag fixes from `cursor/working-except-for-scene-drag-not-yet`

## Pull

```bash
cd /c/Users/rober/SymbolAlgebra
git merge --abort 2>/dev/null
git fetch origin
git checkout cursor/working--scene-drag-1412
git reset --hard origin/cursor/working--scene-drag-1412
```
